# Vercel Blob: create a store at https://vercel.com/dashboard/stores
# then add the token from the store to your project env (and in Vercel project settings).
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxxxx
